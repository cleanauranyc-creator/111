import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { ThemeProvider } from "@/components/theme-provider"
import { SiteHeader } from "@/components/site-header"
import { SiteFooter } from "@/components/site-footer"
import { TopInfoBar } from "@/components/top-info-bar"
import "./globals.css"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "CleanLine - Professional Cleaning Service in NYC",
  description:
    "NYC's most trusted cleaning service. Book your cleaning online in 2 minutes. Standard, deep cleaning, move in/out services. First-time discount $20 off.",
  keywords:
    "NYC cleaning service, house cleaning NYC, apartment cleaning Manhattan, deep cleaning Brooklyn, move out cleaning Queens",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`font-sans antialiased`}>
        <ThemeProvider>
          <div className="flex min-h-screen flex-col">
            <TopInfoBar />
            <SiteHeader />
            <main className="flex-1">{children}</main>
            <SiteFooter />
          </div>
        </ThemeProvider>
      </body>
    </html>
  )
}
